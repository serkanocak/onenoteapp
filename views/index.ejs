<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.2/css/all.css"
    />
    <link rel="stylesheet" href="styles.css" />
    <title>Todo</title>
  </head>
  <header>
    <nav class="navbar navbar-expand-custom navbar-mainbg">
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <a class="nav-link" href="/"><i class="far fa-calendar-alt"></i>Today</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/work">
              <i class="far fa-clone"></i>Work</a>
          </li>
        </ul>
      </div>
    </nav>
  </header>

  <body>
    <main>
      <div class="box" id="heading">
        <h1><%= new Date().toLocaleDateString('en-EN', {
          weekday: 'long',
        }) %> ,<%= new Date().toLocaleDateString('en-EN', {
          month: 'long',
          day: 'numeric'
        }) %> </h1>
      </div>

      <div class="box">
        <% if (locals.tasks) { for( let index = 0; index < tasks.length; index++
        ) { %>
        <div class="item">
          <input type="checkbox" />
          <p><%= tasks[index].desc %> </p>
        </div>
        <% } } %>
        <form class="item" action="/" method="post">
          <input type="text" name="newItem" placeholder="New Item" autocomplete="off"/>
          <button type="submit" name="list" value="">+</button>
        </form>
      </div>
    </main>
    <footer>
      serkan ocak
      </footer>
  </body>
</html>
